hoja de correccion usada: https://rphlr.github.io/42-Evals/Rank05/webserv/


-install siege.

-protect send in poll process (done)

-setup multiple servers with diferente hostnames [how to test?]

-Setup default error page (try to change the error 404).[i think it's necesary cgi]
[i tried error_page keyword]

-creo que el makefile no esta teniendo en cuenta cambios en header webser.hpp

-trying test body size con 100 bytes, el curl propuesto en la correction y un  loren ipsum largo se cayo el servidor.
en la terminal:

Asking file websites/first/index.html
File exists: websites/first/index.html
Something wrong in request body
Error: Unknow method type
Deleting serverhandler
Deleting serverhandler

-se demora mucho en cargar una pagina 

-con estos locations:

    location /change_route {
        allow_methods GET DELETE
        index index.html;
    }

    location /another {
        allow_methods  DELETE GET;
        autoindex off;
        index test.html;
    }

dentro del primer servidor virtual no carga una de las paginas.

-Setup a default file to search for if you ask for a directory
[supongo que se hace en el scope server, pero no recuerdo como hacerlo
]

-checkear que se respetan, y gestioan bien lso archivos con ciertos derechos.

- Demostrar que GET, POST y DELETE funcionan bien.

- UNKNOWN requests should not result in a crash. [creo que se cumple pero quizas falten test]

- checkear status code en todos los casos.

- Upload some file to the server and get it back.

- CGI requirements:

The server is working fine using a CGI.

The CGI should be run in the correct directory for relative path file access.

With the help of the students you should check that everything is working properly.
You have to test the CGI with the "GET" and "POST" methods.

You need to test with files containing errors to see if the error handling
works properly. You can use a script containing an infinite loop or an error;
you are free to do whatever tests you want within the limits of acceptability
that remain at your discretion. The group being evaluated should help you with this.